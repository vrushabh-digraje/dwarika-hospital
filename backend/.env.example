PORT=4000
NODE_ENV=development
MONGODB_URI=mongodb://127.0.0.1:27017/dwarika_hospital_cms
CORS_ORIGIN=http://localhost:5173,http://localhost:5174
UPLOAD_DIR=uploads
MAX_FILE_SIZE_MB=10
PUBLIC_BASE_URL=http://localhost:4000
# Set true to force in-memory MongoDB (or auto-fallback if local Mongo is down)
# USE_MEMORY_MONGO=true
# Future auth (unused for now)
# JWT_SECRET=
# JWT_EXPIRES_IN=7d
